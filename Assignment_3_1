#include <stdio.h>
#include <math.h>

int main ()
{
	float a,b,sum;
	sum=0;
	printf("Enter A : ");
	scanf("%f", &a);
	printf("Enter B : ");
	scanf("%f", &b);
	for (a;a<=b;a++)
	{
		if (fmodf(a,4.0)==0) 
		{
			sum=sum+a;
		}
 	}
 	printf("Sum = %f", sum);
	return 0;
}
